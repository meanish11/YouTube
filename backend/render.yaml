services:
  - type: web
    name: youtube-analyzer-api
    env: node
    region: oregon
    plan: free
    buildCommand: ./build.sh
    startCommand: node server.js
    healthCheckPath: /api/health
    envVars:
      - key: NODE_ENV
        value: production
      - key: FRONTEND_URL
        sync: false
